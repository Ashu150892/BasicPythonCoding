## Decision making
# Syntax
       if Condition:
           statement
    
       if Condition:
           statement
       else:
           statement

## Indentation.
This is one of the biggest differences between Python and Java.
In Java, blocks are defined using { }
In Python, blocks are defined using indentation (spaces).
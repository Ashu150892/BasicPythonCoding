## Variable are used to Store data.
Variable use snake_case in Python like as camel_case is Java.
Varible can use underscore, lower case, snake_case & some time upper case (but not recommended).
Variable can't start with number, can't use keyword (reversed word), can't use hyphens.
Syntax:
variable name = value
'=' is assignment operator which tell that variable name will store this value.